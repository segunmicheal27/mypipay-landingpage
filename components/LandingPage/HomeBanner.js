import ls from "@/styles/LandingPage/HomeBanner.module.css";
import React from "react";
const HomeBanner = () => {
  return (
    <div className={`${ls.banner} p-2`}>
      {/* eslint-disable-next-line @next/next/no-img-element */}
    </div>
  );
};

export default HomeBanner;
